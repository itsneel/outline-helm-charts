{{- if .Values.outline.ingress.enabled -}}
{{- $fullName := include "outline.fullname" . -}}
{{- $svcPort := .Values.outline.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "outline.labels" . | nindent 4 }}
  {{- with .Values.outline.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.outline.ingress.className }}
  ingressClassName: {{ .Values.outline.ingress.className }}
  {{- end }}
  {{- if .Values.outline.ingress.tls }}
  tls:
    {{- range .Values.outline.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.outline.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
